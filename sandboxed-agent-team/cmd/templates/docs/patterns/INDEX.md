# Patterns — Index

Application-agnostic conventions, architecture patterns, recipes, and implementation
guidelines for **Vaadin 24+ with Spring Boot 3+** and Spring Data JPA. Version-
sensitive patterns carry inline "Vaadin ≥X / <X" notes; see [`README.md`](README.md)
→ "Version Compatibility" for the summary matrix. Zero project-specific content.

| Path | Description |
|------|-------------|
| [README.md](README.md) | What the kit provides, how to reuse it, and how to write pattern documents for it |

## Language

Universal language idioms — no framework assumptions.

| Path                                                          | Description                                                                                                                                                                                    |
|---------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [language/singular-form.md](language/singular-form.md)       | Every named artifact (class, table, module, package leaf, route path) should use the singular form, not the plural                                                                             |
| [language/java/conventions.md](language/java/conventions.md) | When writing Java code in this project, follow these style and idiom conventions so every class reads consistently regardless of author                                                         |
| [language/java/lombok.md](language/java/lombok.md)           | When writing an enum with properties, a data POJO, or a class that needs a logger, apply Lombok's annotation subset for the class type so boilerplate is eliminated without proxy failures, lazy-load exceptions, or identity errors       |
| [language/java/comments.md](language/java/comments.md)       | When writing code, default to no comments and add one only when the WHY is non-obvious so comments signal constraints and invariants, not history                                               |
| [language/java/abstraction.md](language/java/abstraction.md) | When the same code shape appears a second time, recognize the extraction window so the duplication does not harden before the third instance makes refactoring expensive                         |

## Structure

How the project and code are organized — independent of any tool or framework.

| Path                                              | Description                                                                                                                                                                                                |
|---------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [structure/modules.md](structure/modules.md)      | When setting up a new application, structure it as a multi-module Maven project with a runtime-scoped `jpaservice` dependency so the UI layer cannot reference JPA types at compile time                   |
| [structure/services.md](structure/services.md)    | When implementing services, use a technology-prefix name, separate queries from mutations, apply `@Transactional` at the service layer, and never pass JPA entities or Vaadin types across the boundary    |

## CI/CD

Build tooling and deployment operations.

| Path                                      | Description                                                                                                                                                                                                               |
|-------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [cicd/deployment.md](cicd/deployment.md)  | When packaging for deployment, produce a fat JAR, run it in a Docker image keyed by Spring profile, and expose `/actuator/health` so each environment deploys from one reproducible artifact                               |
| [cicd/flyway.md](cicd/flyway.md)          | When managing schema changes, use versioned Flyway migration scripts applied on startup so the schema evolves with the application and rollback procedures are documented alongside each change                             |
| [cicd/logging.md](cicd/logging.md)        | When logging application events, use SLF4J with Logback and structured JSON output in non-dev profiles so log entries are parseable by aggregation pipelines                                                               |

## Persistence

Persistence-technology-specific patterns and naming.

| Path                                                                                              | Description                                                                                                                                                                                                                                   |
|---------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [persistence/spring-data-jpa/naming.md](persistence/spring-data-jpa/naming.md)                   | When creating JPA entities, enums, repositories, or projections, use these naming conventions so the persistence-layer role of each artifact is unambiguous from its name alone                                                                |
| [persistence/spring-data-jpa/entity-hierarchy.md](persistence/spring-data-jpa/entity-hierarchy.md) | When creating JPA entities, extend the layered `@MappedSuperclass` hierarchy so identity, optimistic locking, auditing, and `equals`/`hashCode` are inherited consistently and never re-implemented per entity                               |
| [persistence/spring-data-jpa/jpa-config.md](persistence/spring-data-jpa/jpa-config.md)           | When configuring Spring Data JPA, disable OSIV and declare explicit entity and repository scan packages so data loading is transaction-scoped and all entities are discovered regardless of package layout                                     |
| [persistence/spring-data-jpa/entity-design.md](persistence/spring-data-jpa/entity-design.md)     | When writing entity classes, declare `@ManyToOne` as lazy, store enums as STRING, and choose cascade strategy based on child lifecycle so Hibernate does not silently issue extra queries or corrupt data                                      |
| [persistence/spring-data-jpa/entity-lombok.md](persistence/spring-data-jpa/entity-lombok.md)     | When a JPA entity has accessor or constructor boilerplate, apply only `@NoArgsConstructor`, `@Getter`, and `@Setter` and suppress Lombok on managed collection fields so Hibernate proxies, lazy loading, and bidirectional relationships do not cause silent runtime failures |
| [persistence/spring-data-jpa/projections.md](persistence/spring-data-jpa/projections.md)         | When querying repositories, return interface projections for each UI context so only the needed fields are fetched and field renames are caught at compile time                                                                                |
| [persistence/spring-data-jpa/queries.md](persistence/spring-data-jpa/queries.md)                 | When writing queries and mutations, use named parameters, load managed entities for updates, and prefer `@EntityGraph` for joins so the persistence layer does not silently issue extra queries or overwrite concurrent changes                 |

## Security

| Path                                                                                                            | Description                                                                                                                                                                                                                         |
|-----------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [security/spring/vaadin-web-security.md](security/spring/vaadin-web-security.md)                               | When configuring Spring Security for Vaadin, extend `VaadinWebSecurity` so session fixation, CSRF handling, and the `AnnotatedViewAccessChecker` integration are configured correctly without re-declaring framework defaults         |
| [security/spring/session.md](security/spring/session.md)                                                        | When configuring session management, set the inactivity timeout and cookie security flags so idle sessions expire and the session cookie is protected from theft and cross-site requests                                              |
| [security/spring/security-headers.md](security/spring/security-headers.md)                                      | When adding HTTP security headers, configure them through `VaadinWebSecurity.configure(...)` and respect Vaadin's required CSP relaxations so the client runtime is not broken                                                       |
| [security/authentication/bcrypt.md](security/authentication/bcrypt.md)                                          | When storing user passwords, hash with BCrypt at work factor ≥ 10 so credentials cannot be recovered if the database is compromised                                                                                                  |
| [security/authentication/password-strength.md](security/authentication/password-strength.md)                    | When validating passwords, reject by entropy rather than character-class rules so weak passwords are blocked without frustrating users with arbitrary composition requirements                                                        |
| [security/authentication/failure-messages.md](security/authentication/failure-messages.md)                      | When reporting authentication failures or account lockouts, use the same generic message so accounts cannot be enumerated and lockout policy cannot be reverse-engineered                                                             |
| [security/authentication/rate-limiting.md](security/authentication/rate-limiting.md)                            | When a login endpoint receives repeated failures from the same IP, apply a temporary block so brute-force attacks are slowed without exposing the threshold                                                                          |
| [security/authorization/view-access.md](security/authorization/view-access.md)                                  | When creating a `@Route` view, add exactly one access annotation so `AnnotatedViewAccessChecker` has an unambiguous rule for every route                                                                                             |
| [security/authorization/role-constants.md](security/authorization/role-constants.md)                            | When defining application roles, declare them as a `UserRole` enum with `ROLE_*` constants so role names are referenced by constant rather than string literal                                                                       |
| [security/authorization/layout-access.md](security/authorization/layout-access.md)                              | When annotating the main layout, use `@PermitAll` (with `anyRequest` override on Vaadin ≥25) so anonymous users hitting protected routes are cleanly redirected to login                                                             |
| [security/authorization/role-based-rendering.md](security/authorization/role-based-rendering.md)                | When a component is not authorized for the current user, never construct or add it to the layout so there is no DOM artifact the user can re-enable via browser inspector                                                            |
| [security/authorization/self-editing.md](security/authorization/self-editing.md)                                | When an action on a user's own record would leave the system inconsistent, enforce the restriction in the service layer so it holds against programmatic callers                                                                     |
| [security/data-protection/secrets.md](security/data-protection/secrets.md)                                      | When configuring application secrets, inject via environment variables so credentials cannot be extracted from the repository history                                                                                                |
| [security/data-protection/tls.md](security/data-protection/tls.md)                                              | When deploying, require TLS for all client-server communication so credentials and session tokens cannot be intercepted in transit                                                                                                   |
| [security/data-protection/pii-logging.md](security/data-protection/pii-logging.md)                              | When logging application events, exclude PII from INFO-level and below so personal data is not captured in aggregated log pipelines                                                                                                  |
| [security/data-protection/file-upload.md](security/data-protection/file-upload.md)                              | When accepting file uploads, validate type and size before storage so malicious or oversized files are rejected at the boundary                                                                                                      |
| [security/data-protection/error-leakage.md](security/data-protection/error-leakage.md)                          | When handling errors, suppress internal details from all responses so stack traces, file paths, and server version cannot be extracted by an attacker                                                                                |
| [security/data-protection/xss.md](security/data-protection/xss.md)                                              | When rendering user-supplied data in Vaadin components, rely on the component layer's built-in output escaping so no additional escaping is needed for standard components                                                           |
| [security/data-protection/dependency-scanning.md](security/data-protection/dependency-scanning.md)              | When building in CI, run OWASP Dependency-Check so known-vulnerable libraries are caught before they ship to production                                                                                                              |
| [security/spring/recipes/audited-principal.md](security/spring/recipes/audited-principal.md)                    | When integrating JPA auditing with Spring Security, use this recipe to wire a principal that carries the user key for `@CreatedBy`/`@LastModifiedBy`                                                                                |
| [security/passkey/recipes/passkey.md](security/passkey/recipes/passkey.md)                                      | When adding passkey (WebAuthn) authentication, follow this recipe to configure the Spring Security integration and credential storage                                                                                                |
| [security/oidc/recipes/oidc-sso.md](security/oidc/recipes/oidc-sso.md)                                          | When integrating an OIDC/SSO provider, follow this recipe to configure Spring Security's OAuth2 login and map claims to application roles                                                                                           |
| [security/form-login/recipes/form-login.md](security/form-login/recipes/form-login.md)                          | When adding username/password form login, follow this recipe to configure Spring Security's form login with BCrypt and entropy-based password validation                                                                            |
| [security/form-login/recipes/conditional-auth.md](security/form-login/recipes/conditional-auth.md)              | When the application must support multiple auth methods selectable at deploy time, follow this recipe to gate each method via `application.properties` flags                                                                        |

## UI

| Path                                                                                    | Description                                                                                                                                                                                                          |
|-----------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [ui/vaadin/spring-component.md](ui/vaadin/spring-component.md)                          | When registering a Spring bean in a Vaadin-facing module, use `@SpringComponent` instead of `@Component` so the bean participates in Vaadin's lifecycle and does not silently fail when later gaining a Vaadin scope  |
| [ui/vaadin/spring-boot-config.md](ui/vaadin/spring-boot-config.md)                      | When configuring Spring Boot for Vaadin, declare `vaadin.allowed-packages` and add `vaadin-dev` as optional so Vaadin add-on components resolve and dev tools are excluded from production JARs                       |
| [ui/vaadin/layout-setup.md](ui/vaadin/layout-setup.md)                                  | When creating the main layout, use `@Layout` on Vaadin 24.1+ or `layout = MainLayout.class` per `@Route` on 24.0, and never add `implements RouterLayout` so the router finds the layout without a redundant interface declaration |
| [ui/vaadin/session-scope.md](ui/vaadin/session-scope.md)                                | When a bean holds per-session state Push callbacks must access, use `@Scope(VaadinSessionScope.VAADIN_SESSION_SCOPE_NAME)` with `ScopedProxyMode.INTERFACES` so the bean is resolvable from both HTTP request and Push threads              |
| [ui/vaadin/views.md](ui/vaadin/views.md)                                                | When creating a Vaadin view, use `Composite<T>`, place it in its own sub-package, and annotate it with `@Menu` so navigation and package structure stay consistent                                                    |
| [ui/vaadin/constructor-init.md](ui/vaadin/constructor-init.md)                          | When building a Vaadin view constructor, keep all assembly inline with sectioning comments and group code by operation type (component inits → signal defs → binder bindings → value settings) so the constructor reads as a single top-to-bottom narrative |
| [ui/vaadin/dialog-delegation.md](ui/vaadin/dialog-delegation.md)                        | When building a dialog, wrap `Dialog` via delegation and implement the `NonComponent` event infrastructure so the class exposes a focused public API with typed listener registration instead of Dialog's full interface |
| [ui/vaadin/signals.md](ui/vaadin/signals.md)                                            | When managing non-`Binder` component state on Vaadin ≥25.1, use Vaadin Signals as the default state primitive and name Signal fields with their type suffix so reactive changes propagate without manual listener wiring |
| [ui/vaadin/component-naming.md](ui/vaadin/component-naming.md)                          | When declaring a Vaadin UI component as a class field, always include the component type as a name suffix so field types are unambiguous                                                           |
| [ui/vaadin/layout-diagram.md](ui/vaadin/layout-diagram.md)                              | Every Vaadin view and custom component class should have a layout diagram in its class Javadoc so the intended structure is visible without running the application                                                    |
| [ui/vaadin/uimodel.md](ui/vaadin/uimodel.md)                                            | Types in `{app}-uimodel` should be named for their UI context, implement `HasCaption` when used in selection components, and implement capability interfaces when generic UI components bind to their structure         |
| [ui/vaadin/client-details-service.md](ui/vaadin/client-details-service.md)              | When service-layer code needs the user's browser timezone, implement `ClientDetailsService` so non-UI modules convert `Instant` to local types without a Vaadin dependency                                             |
| [ui/vaadin/datetime.md](ui/vaadin/datetime.md)                                          | When displaying date or time values in a Vaadin view, format them through `DateTimeUtil` with the locale resolved per-call from `UI.getCurrent()` so rendered values reflect the user's locale                        |
| [ui/vaadin/theming.md](ui/vaadin/theming.md)                                            | When styling the application, use Lumo CSS custom properties and `LumoUtility` classes so the theme stays consistent and maintainable without custom CSS                                                              |
| [ui/vaadin/navigation.md](ui/vaadin/navigation.md)                                      | When building navigation, use `AppLayout` + `SideNav` with `@Menu`-annotated routes so the nav structure is declarative and conditionally rendered by role                                                           |
| [ui/vaadin/quick-filter.md](ui/vaadin/quick-filter.md)                                  | When adding text search to a grid view, implement a Quick Filter — a `TextField` with `LAZY` value-change mode and case-insensitive partial match across the specified columns — so every grid view has consistent search behavior |
| [ui/vaadin/avatar.md](ui/vaadin/avatar.md)                                              | When displaying a user profile photo or entity logo, use the Vaadin `Avatar` component so initials are shown automatically when no image is set and display is consistent across views                                |
| [ui/vaadin/grid-columns.md](ui/vaadin/grid-columns.md)                                  | When configuring grid columns, declare each column explicitly with a header, sort property, and appropriate width strategy so column widths are predictable and sortable columns are declared consistently              |
| [ui/vaadin/forms.md](ui/vaadin/forms.md)                                                | When building a Vaadin form, use `Binder` for field-to-model binding and `FormLayout` for the container so fields are bound consistently, validation errors appear inline, and the layout adapts to screen width       |
| [ui/vaadin/notification-toast.md](ui/vaadin/notification-toast.md)                      | When showing an outcome that cannot be displayed inline, use a Vaadin `Notification` toast with 3 s for success or 5 s for error at `BOTTOM_END` so feedback is non-blocking and temporally appropriate               |
| [ui/vaadin/service-error-handling.md](ui/vaadin/service-error-handling.md)              | When a view calls a service method, branch on `ValidationException`, `EntityNotFoundException`, and `Exception` separately so user-authored validation messages surface directly and internal details are never shown   |
| [ui/vaadin/confirmation-dialog.md](ui/vaadin/confirmation-dialog.md)                    | When a destructive action is triggered, show a `ConfirmDialog` that states the consequence in plain language before executing so the action cannot be taken accidentally                                               |
| [ui/vaadin/loading-indicator.md](ui/vaadin/loading-indicator.md)                        | When triggering an operation longer than 200 ms, disable the triggering control and show a loading cue before completion so the user knows the action is in progress                                                  |
| [ui/vaadin/conditional-rendering.md](ui/vaadin/conditional-rendering.md)                | When a control has conditional availability, use 'do not generate' for authorization, `setVisible(false)` for inapplicable, or `setEnabled(false)` with tooltip for blocked so no auth gate can be bypassed via the browser DOM |
| [ui/vaadin/responsive.md](ui/vaadin/responsive.md)                                      | When building views, follow these responsive layout patterns so the UI adapts correctly to mobile, tablet, and desktop breakpoints                                                                                    |
| [ui/vaadin/error-views.md](ui/vaadin/error-views.md)                                    | When handling navigation and server errors, use these error view patterns so error pages are consistent and do not leak sensitive information                                                                          |
| [ui/vaadin/recipes/base-view.md](ui/vaadin/recipes/base-view.md)                        | When creating the base view scaffold, follow this recipe to set up `MainLayout`, `AppLayout`, and the shared navigation chrome                                                                                        |
| [ui/vaadin/recipes/item-browser.md](ui/vaadin/recipes/item-browser.md)                  | When building a grid-plus-editor view, follow this recipe to implement the `ItemBrowser` pattern with quick filter, record count, and inline edit dialog                                                              |
| [ui/vaadin/recipes/app-icon.md](ui/vaadin/recipes/app-icon.md)                          | When adding an application icon (favicon and PWA icon), follow this recipe                                                                                                                                           |
| [ui/vaadin/recipes/view-icon.md](ui/vaadin/recipes/view-icon.md)                        | When adding icons to side-nav menu entries, follow this recipe to supply icons via `@Menu`                                                                                                                           |
| [ui/vaadin/recipes/java-lit-bridge.md](ui/vaadin/recipes/java-lit-bridge.md)            | When a Vaadin view needs a custom Lit web component, follow this recipe to bridge the Java and TypeScript sides                                                                                                       |

## Design

| Path                                                                                           | Description                                                                                                                                                                                   |
|------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [design/figma/recipes/figma-to-lumo-theme.md](design/figma/recipes/figma-to-lumo-theme.md)    | When translating a Figma design system, extract variables via `get_variable_defs`, map each token to a Lumo CSS custom property, and emit only non-default declarations so the implementation tracks the design source without overriding matching Lumo defaults |
| [design/figma/recipes/figma-to-vaadin.md](design/figma/recipes/figma-to-vaadin.md)            | When translating a Figma frame into a Vaadin view, extract design context via Figma MCP first, resolve Figma/`docs/reqs` discrepancies before coding, and map design primitives to Vaadin components plus Lumo tokens so the result matches the design's semantic structure |

## Testing

| Path                                                                                          | Description                                                                                                                                                                                       |
|-----------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [testing/pyramid.md](testing/pyramid.md)                                                      | When planning test coverage, structure tests across unit, browserless UI, and E2E tiers so fast in-process tests run per-commit and slow browser tests run only at the pre-PR gate                 |
| [testing/unit.md](testing/unit.md)                                                            | When writing unit tests, use JUnit 5 with Mockito and AssertJ assertions named in `subject_verb_condition` form so tests read as behavioral specifications                                         |
| [testing/browserless-ui.md](testing/browserless-ui.md)                                        | When writing in-process UI tests, use `BrowserlessTest` or `SpringBrowserlessTest` so form submission, validation, and grid interactions are exercised without a real browser                       |
| [testing/page-object.md](testing/page-object.md)                                              | When writing UI tests, hide component traversal behind a Page Object class so tests survive layout changes without modification                                                                     |
| [testing/repository.md](testing/repository.md)                                                | When testing JPA repositories, use `@DataJpaTest` with `@Transactional` rollback and H2 in PostgreSQL mode so each test runs in isolation against a production-equivalent schema                   |
| [testing/acceptance-tracing.md](testing/acceptance-tracing.md)                                | When writing tests, trace every acceptance criterion to at least one passing test so coverage gaps are visible and "done" means the specified behavior is verified                                  |
| [testing/recipes/testbench-browserless.md](testing/recipes/testbench-browserless.md)          | When writing fast UI tests without a browser, follow this recipe to set up Vaadin's browserless `ComponentTester<T>` infrastructure                                                               |
| [testing/recipes/testbench-e2e-server.md](testing/recipes/testbench-e2e-server.md)            | When running TestBench E2E tests, follow this recipe to configure the JUnit 5 server extension with concurrency limiting                                                                          |
| [testing/recipes/testbench-e2e-parallel.md](testing/recipes/testbench-e2e-parallel.md)        | When parallelizing TestBench E2E tests, follow this recipe to configure `@Execution` and the concurrency limit so tests run fast without saturating the server                                    |