# Async Operation Notifications

When a background operation completes and the initiating user needs feedback,
push a `Notification` toast via `UI.access()` so the result reaches the client
without a page refresh or manual polling.

> Pattern to be documented.