# Headroom Scrolling

When the application uses a touch-optimized layout, hide the top header,
secondary section tabs, and bottom nav bar on downward scroll and reveal
them on upward scroll so the full viewport height is available for content.

> Recipe to be documented.
