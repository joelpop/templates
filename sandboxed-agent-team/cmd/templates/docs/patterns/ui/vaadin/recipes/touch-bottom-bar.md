# Touch-Optimized Bottom Navigation Bar

When initializing navigation on a touch device, render the top-level routes
as a bottom icon bar — up to five entries, with the fifth replaced by a
"More" overflow when there are more than five — instead of the drawer
`SideNav` so primary destinations are within thumb reach.

> Recipe to be documented.
