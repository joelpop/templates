# Touch Secondary Tab Navigation

When a touch navigation destination has sub-routes, render the sub-level
routes as a secondary tab bar in `MainLayout` — not inside the view — so
nested navigation is accessible at the layout level and collapses with the
top header and bottom bar in the headroom effect.

> Recipe to be documented.
