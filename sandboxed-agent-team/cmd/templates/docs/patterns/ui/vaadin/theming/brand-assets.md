# Logo and Brand Assets

When including brand assets (logo, custom fonts), place them in `src/main/frontend/` and reference via `@StyleSheet` or `@JsModule` on `Application.java`, or from the custom CSS stylesheet.
